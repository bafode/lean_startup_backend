version: '3'

services:
  node-app:
    build: .
    image: res-node-beehive-backend
    environment:
      - MONGODB_URL=mongodb+srv://Bafode224:B%40fode224gn@cluster0.f3ko5ib.mongodb.net/beehavedb?retryWrites=true&w=majority
    ports:
      - '4000:4000'
    depends_on:
      - mongodb
    volumes:
      - .:/usr/src/res-node-boilerplate
    networks:
      - node-network

  mongodb:
    image: mongo:4.2.1-bionic
    ports:
      - '27017:27017'
    volumes:
      - dbdata:/data/db
    networks:
      - node-network

volumes:
  dbdata:

networks:
  node-network:
    driver: bridge